/* In this SQL, I'm querying a database with multiple tables in it to quantify statistics about customer and order data */

/* 1. How many orders were placed in January? */
SELECT COUNT(Quantity)
FROM BIT_DB.JanSales;

/* 2. How many of those orders were for an iPhone? */
SELECT COUNT(jan.Quantity)
FROM BIT_DB.JanSales jan
WHERE jan.Product = 'iPhone';

/* 3.Select the customer account numbers for all the orders that were placed in February */
SELECT cus.acctnum
FROM BIT_DB.FebSales feb
LEFT JOIN BIT_DB.customers cus
ON feb.orderID = cus.order_id
WHERE length(order_id)=6
AND order_id <> 'Order Id';

/* 4.Which product was the cheapest one sold in January, and what was the price? */
SELECT Product, MIN(price)
FROM BIT_DB.JanSales;

/* 5.What is the total revenue for each product sold in January? (Revenue can be calculated using the number of products sold and the price of the products) */
SELECT Product, SUM(Quantity)*Price AS Revenue
FROM BIT_DB.JanSales 
GROUP BY Product;

/* 6.Which products were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what was the total revenue? */
SELECT Product, SUM(Quantity), SUM(Quantity)*Price AS Revenue
FROM BIT_DB.FebSales 
WHERE location='548 Lincoln St, Seattle, WA 98101'
GROUP BY Product;

/* 7.Find how many items each customer who bought more than 2 items bought */
SELECT COUNT(cus.acctnum), COUNT(feb.Quantity)
FROM BIT_DB.customers cus
JOIN BIT_DB.FebSales feb
ON cus.order_id = feb.orderID
WHERE length(order_id)=6
AND order_id <> 'Order Id'
GROUP BY cus.acctnum
HAVING COUNT(feb.Quantity)>2;

/* 8.How many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers? */
SELECT COUNT(DISTINCT cus.acctnum) AS customer_count, AVG(feb.Quantity*feb.Price) AS average_spending
FROM BIT_DB.FebSales AS feb
LEFT JOIN BIT_DB.customers AS cus
ON cus.order_id = feb.orderID
WHERE feb.quantity >2 
AND length(order_id)=6
AND order_id <> 'Order Id';

/* 9. List all the products sold in Los Angeles in February, and include how many of each were sold */
SELECT Product, COUNT(Quantity) AS number_of_items
FROM BIT_DB.FebSales
WHERE location LIKE '%Los Angeles%'
GROUP BY Product;

/* 10. List all the products sold in Los Angeles in February, and include how many of each were sold */
SELECT Product, COUNT(Quantity) AS number_of_items
FROM BIT_DB.FebSales
WHERE location LIKE '%Los Angeles%'
GROUP BY Product;

/* 11. Which locations in New York received at least 3 orders in January, and how many orders did they each receive? (Hint: use HAVING) */
SELECT Product, COUNT(Quantity) AS num_of_orders
FROM BIT_DB.JanSales
WHERE location LIKE '%New York City%'
GROUP BY Product
HAVING num_of_orders >=3;

/* 12. How many of each type of headphone were sold in February? */
SELECT DISTINCT(Product), COUNT(Quantity)
FROM BIT_DB.FebSales 
WHERE Product LIKE '%Headphones%'
GROUP BY Product;

/* 13. What was the average amount spent per account in February? 
(Hints: For this question, we want the average amount spent / number of accounts, not the amount spent by each account. 
To multiply, you can use the * symbol, and to divide, you can use the / symbol) */
SELECT SUM(feb.Quantity*feb.Price)/COUNT(cus.acctnum) AS avg_spending_per_acct
FROM BIT_DB.FebSales AS feb
LEFT JOIN BIT_DB.customers AS cus
ON feb.orderID = cus.order_id
WHERE LENGTH(orderID)=6
AND orderID <> 'OrderID';

/* 14. What was the average quantity of products purchased per account in February? (Hint: just like question 3, we want the overall average, not the average for each account individually) */
SELECT SUM(feb.quantity)/COUNT(acctnum) AS avg_quantity_per_acct
FROM BIT_DB.FebSales AS feb
LEFT JOIN BIT_DB.customers AS cus 
ON feb.orderid = cus.order_id
WHERE LENGTH(order_id) = 6
AND orderID <> 'OrderID';

/* 15. Which product brought in the most revenue in January and how much revenue did it bring in total? */
SELECT Product, SUM(Price*Quantity) AS revenue
FROM BIT_DB.JanSales 
GROUP BY Product
ORDER BY revenue DESC
LIMIT 1; 
